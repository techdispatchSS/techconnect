package silver.solutions.techconnect.entity;

/** PRD FR-04 — one invited technician's outcome within a dispatch. */
public enum DispatchResponseStatus {
    PENDING,
    ACCEPTED,
    DECLINED,
    NO_RESPONSE
}
