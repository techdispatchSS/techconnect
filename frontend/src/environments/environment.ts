export const environment = {
  production: true,
  apiUrl: '/api',
  // Browser key for Google Places address search. Restrict it to your site's referrers and to
  // the Places API (New) in Google Cloud; while empty the address form is plain manual entry.
  googleMapsApiKey: '',
};
